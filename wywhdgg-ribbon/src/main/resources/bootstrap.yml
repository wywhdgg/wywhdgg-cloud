###定义实例名
spring:
  application:
    name: spring-cloud-ribbon-consumer
server:
  port: 8092

##禁止向eureka注册
eureka:
  client:
    enabled: false


##脱离eureka来完成服务调用
helloclient:
  ribbon:
    listOfServers: localhost:8001,localhost:8002